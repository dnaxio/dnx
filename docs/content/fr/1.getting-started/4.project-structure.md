---
title: Quickstart
description: Créez et déployez votre premier projet en 5 minutes.
navigation:
  icon: i-lucide-rocket
---

# Quickstart

## 1. Initialiser un projet

```bash
mkdir my-app && cd my-app
dnx init --name my-app
```

Cela crée un fichier `.dnax/dnx.yaml` avec une configuration de base.

## 2. Configurer vos variables

Créez un fichier `.env` à la racine :

```bash
# .env
PROD_HOST=mon-serveur.example.com
STAGING_HOST=staging.example.com
```

DNX charge automatiquement ces variables. Utilisez `$PROD_HOST` dans votre `dnx.yaml`.

## 3. Configurer vos serveurs

Éditez `.dnax/dnx.yaml` pour renseigner vos serveurs :

```yaml
environments:
  staging:
    servers:
      - name: staging-1
        host: 10.0.1.10
        user: deploy
        port: 22
        key_path: ~/.ssh/id_ed25519
```

## 4. Définir votre workload

```yaml
workloads:
  - name: web
    driver: flox
    start_cmd: "bun run server.ts"
    ports:
      - 3000
    env:
      NODE_ENV: "production"
      DATABASE_URL: "${secret:DATABASE_URL}"
    build:
      steps:
        - run: bun install
        - run: bun run build
    health:
      type: http
      endpoint: /health
```

## 5. Configurer les secrets

```bash
dnx secrets init
dnx secrets set DATABASE_URL=postgres://user:pass@host:5432/db --env production
```

## 6. Déployer

```bash
# Build (exécute les étapes de build via flox)
dnx build web

# Déployer un workload en staging
dnx deploy web --env staging --tag v1

# Déployer tous les workloads
dnx deploy --all --env staging --tag v1

# Déployer en production (blue-green)
dnx deploy web --env production --tag v1 --strategy blue-green
```

## 7. Vérifier

```bash
# Statut du workload
dnx workload status web --env staging

# Logs
dnx workload logs web --env staging --follow

# Health check
dnx health check web --env staging
```

## Prochaine étape

Comprendre en détail le [fichier dnx.yaml](/configuration/dnx-yaml).
